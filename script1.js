// This is the boilerplate template
export var Name = (name) => { // Name is what we call the entire exported function, just the way of naming. name is a sentence.
    return "My name is " + name;
};

export var Address = (address) => {
    return "I live in " + address;
};

export var Age = (age) => {
    return "my age is " + age;
};